services:
  - type: web
    name: washwish
    env: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate
      python manage.py populate_data
    startCommand: gunicorn detergee.wsgi:application --bind 0.0.0.0:$PORT --workers 3
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: PYTHON_VERSION
        value: "3.9.18"
      - key: DATABASE_URL
        fromDatabase:
          name: washwish-db
          property: connectionString
      - key: EMAIL_PASSWORD
        value: "your-gmail-app-password"
      - key: ALLOWED_HOSTS
        value: "*"

databases:
  - name: washwish-db
    databaseName: washwish
    user: washwish
    plan: free